<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.csci5115.activities">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".Host_Code_Display" />
        <activity android:name=".Join_Code_enter" />
        <activity android:name=".New_Playlist" />
        <activity android:name=".Pop_PlayListActivity" />
        <activity android:name=".PlayListActivity" />
        <activity android:name=".Search_Enter" />
        <activity android:name=".Search_Result" />
        <activity android:name=".Host_Playlist_Manage" />
        <activity android:name=".Summary" />
        <activity android:name=".Guest_Playlist_Manage"></activity>
    </application>

</manifest>